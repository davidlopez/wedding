<div class="snap-y snap-mandatory h-screen overflow-scroll scroll-smooth">
  <div class="snap-center shrink-0">
    <%= render "shared/intro" %>
  </div>
  <div class="snap-center shrink-0">
    <%= render "shared/travel" %>
  </div>
  <div class="snap-center shrink-0">
    <% if device != "mobile" %>
      <%= render "shared/gallery" %>
    <% else %>
      <%= render "shared/gallery-mobile" %>
    <% end %>
  </div>
</div>